apiVersion: v1
kind: Service
metadata:
  name: {{ include "memory-game-chart.fullname" . }}
  labels:
    {{- include "memory-game-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "memory-game-chart.selectorLabels" . | nindent 4 }}
